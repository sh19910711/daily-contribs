{
  "name": "client",
  "version": "1.0.0",
  "scripts": {
    "watch": "$(npm bin)/webpack --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "webpack": "^1.13.2"
  }
}
